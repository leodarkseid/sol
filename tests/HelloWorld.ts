import { expect } from "chai";


describe("My Title", () => {
    it("should work", () => {
        return true;
    })
    it("1 + 1 should equal to 2", () =>{
        expect(1 + 1).to.equal(2);
    })
})